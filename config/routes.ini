; <?php die; ?>
; This line prevents accidentally serving this file.

; Do not edit this file. Instead, edit its counterpart at
; /application/config/routes.ini.

; This config file assigns an 'authentication level' to each possible
; route. This authentication level is compared against that of the logged
; in user, if any, by the _te->is_authorized(). In the simplest case,
; these are just integer numbers, where user's level should be equal or
; less than the level route he is requesting.

; Below, the default settings (change by overriding this file, not by changing
; this file)

; accept all requests at /home/index
[home]
index = 2

; accept all css / js / img requests
[application]
css = 2;
js = 2;
img = 2;
